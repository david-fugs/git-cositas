//configuracion

git config --global 


 git config --global user.name David lop"

email: 
git config --global user.email fulgore0142@gmail.com

vs editor por defecto 
 git config --global core.editor "code --wait"

git config --goblal core.autocrlf true

carpetas
cd entrar 
salir cd ..

crear carpetas
mkdir nombreAplicacion

git init obvio pues

git add . o puede ser.txt o las extensiones 

git commit -m "primer commit"

para eliminar
rm nombreArchivo.txt o la extension pues


recuperar archivos
git restore --staged nombreArchivo.js

cambiar nombres de archivos
git mv elnombrearchivo.js   elNombrequeruiro.js

informacion 
git log     solo o con --oneline

saber en que rama estamos
git branch

como crear ramas
 git checkout -b nombreDelaRama

cat nombreArchivo.txt para ver toda la informacion del archivos

como pasar informacion de las ramas